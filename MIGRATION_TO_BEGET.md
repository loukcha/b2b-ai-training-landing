# 📦 Инструкция: Переезд проекта с poehali.dev на хостинг Beget

## Что вам понадобится
- Аккаунт на Beget.com
- FTP-клиент (FileZilla) или файловый менеджер Beget
- 15-20 минут времени

---

## 🚀 Шаг 1: Скачивание билда проекта

1. Откройте ваш проект в редакторе poehali.dev
2. Нажмите кнопку **"Скачать"** в верхнем меню
3. Выберите **"Скачать билд"**
4. Сохраните ZIP-архив на компьютер
5. Распакуйте архив в отдельную папку

**Что внутри:**
```
dist/
├── index.html          ← главная страница
├── assets/
│   ├── index-xxxxx.js  ← JavaScript код
│   ├── index-xxxxx.css ← стили
│   └── ...другие файлы
└── favicon.svg
```

---

## 📝 Шаг 2: Создание PHP-обработчика формы

1. Создайте файл `send.php` в текстовом редакторе (Notepad++, VS Code или любом другом)
2. Скопируйте в него следующий код:

```php
<?php
header('Content-Type: application/json; charset=utf-8');
header('Access-Control-Allow-Origin: *');
header('Access-Control-Allow-Methods: POST, OPTIONS');
header('Access-Control-Allow-Headers: Content-Type');

// Обработка preflight запроса
if ($_SERVER['REQUEST_METHOD'] === 'OPTIONS') {
    http_response_code(200);
    exit;
}

function post($k){
    return isset($_POST[$k]) ? htmlspecialchars(trim($_POST[$k]), ENT_QUOTES | ENT_SUBSTITUTE, 'UTF-8') : '';
}

// Получение данных из JSON (React отправляет JSON, не form-data!)
$input = file_get_contents('php://input');
$data = json_decode($input, true);

$name = isset($data['name']) ? htmlspecialchars(trim($data['name']), ENT_QUOTES | ENT_SUBSTITUTE, 'UTF-8') : '';
$email = isset($data['email']) ? htmlspecialchars(trim($data['email']), ENT_QUOTES | ENT_SUBSTITUTE, 'UTF-8') : '';
$phone = isset($data['phone']) ? htmlspecialchars(trim($data['phone']), ENT_QUOTES | ENT_SUBSTITUTE, 'UTF-8') : '';

// Валидация
$errors = [];
if(mb_strlen($name) < 2) $errors[] = 'Имя слишком короткое';
if(!filter_var($email, FILTER_VALIDATE_EMAIL)) $errors[] = 'Неверный email';
if(!preg_match('/^[+0-9()\-\s]{6,20}$/', $phone)) $errors[] = 'Неверный телефон';

if(!empty($errors)){
    echo json_encode(['success'=>false,'error'=>implode('; ',$errors)], JSON_UNESCAPED_UNICODE);
    exit;
}

// Отправка письма
$to = 'email@btbsales.ru'; // ← ЗАМЕНИТЕ НА ВАШ EMAIL!
$subject = 'Новая заявка: AI Accelerator';
$site = $_SERVER['HTTP_HOST'] ?? 'b2baisales.ru';

$body = "Новая заявка с сайта\n\n";
$body .= "Сайт: $site\n";
$body .= "Имя: $name\n";
$body .= "Email: $email\n";
$body .= "Телефон: $phone\n";
$body .= "IP: " . ($_SERVER['REMOTE_ADDR'] ?? '') . "\n";
$body .= "Дата: " . date('d.m.Y H:i:s') . "\n";

$headers = [
    'From: noreply@'.$site,
    'Reply-To: '.$email,
    'MIME-Version: 1.0',
    'Content-Type: text/plain; charset=utf-8'
];

$ok = mail($to, $subject, $body, implode("\r\n", $headers));

if($ok){
    echo json_encode(['success'=>true], JSON_UNESCAPED_UNICODE);
} else {
    // Логирование ошибок
    $log = __DIR__ . '/mail_log.txt';
    file_put_contents($log, date('c')." Ошибка отправки. Данные: ".print_r($data,true)."\n", FILE_APPEND);
    echo json_encode(['success'=>false,'error'=>'Не удалось отправить письмо'], JSON_UNESCAPED_UNICODE);
}
?>
```

3. **ВАЖНО:** Замените `email@btbsales.ru` на ваш реальный email!
4. Сохраните файл как `send.php`

---

## 🔧 Шаг 3: Изменение адреса отправки формы

Теперь нужно указать сайту, куда отправлять форму.

1. Откройте папку `dist/assets/`
2. Найдите файл `index-xxxxx.js` (название с хешем может отличаться)
3. Откройте его в текстовом редакторе
4. Нажмите `Ctrl+F` и найдите: `https://functions.poehali.dev/0dda9b16-5710-45b1-a07c-ca0a458a0ed0`
5. Замените на: `https://ваш-домен.ru/send.php` или `/send.php` (если файлы в корне сайта)

**Пример:**
```javascript
// Было:
fetch('https://functions.poehali.dev/0dda9b16-5710-45b1-a07c-ca0a458a0ed0', {

// Стало:
fetch('/send.php', {
```

6. Сохраните файл

---

## 📤 Шаг 4: Загрузка на хостинг Beget

### Вариант А: Через файловый менеджер Beget

1. Войдите в панель управления Beget
2. Перейдите в **"Файловый менеджер"**
3. Откройте папку вашего домена (например, `ваш-домен.ru/public_html/`)
4. Загрузите ВСЕ файлы из папки `dist/`:
   - `index.html`
   - папку `assets/` целиком
   - `favicon.svg`
   - файл `send.php`

### Вариант Б: Через FTP (FileZilla)

1. Скачайте FileZilla: https://filezilla.ru/
2. В панели Beget найдите данные FTP:
   - Хост: обычно `ftp.ваш-домен.ru`
   - Логин: ваш логин
   - Пароль: ваш пароль FTP
   - Порт: 21
3. Подключитесь через FileZilla
4. Откройте папку `public_html/` на сервере
5. Перетащите все файлы из `dist/` на сервер

---

## ✅ Шаг 5: Проверка работы

1. Откройте ваш сайт в браузере: `https://ваш-домен.ru`
2. Прокрутите до формы заявки
3. Заполните форму тестовыми данными
4. Нажмите "Отправить заявку"
5. Проверьте email (указанный в `send.php`)

**Если письмо НЕ пришло:**
- Проверьте папку "Спам"
- Убедитесь, что в `send.php` указан правильный email
- Проверьте файл `mail_log.txt` на сервере (там будут ошибки)

---

## 🔒 Шаг 6: Настройка домена и SSL (опционально)

Если используете свой домен:

1. В панели Beget перейдите в **"Домены и поддомены"**
2. Добавьте ваш домен
3. Укажите DNS-серверы Beget в регистраторе домена
4. Включите **бесплатный SSL-сертификат** от Let's Encrypt
5. Дождитесь обновления DNS (до 24 часов, обычно 1-2 часа)

---

## 📊 Шаг 7: Проверка Яндекс.Метрики

Яндекс.Метрика уже встроена в код (счетчик 105280026).

1. Зайдите в Яндекс.Метрику: https://metrika.yandex.ru
2. Откройте счетчик 105280026
3. Проверьте, что данные поступают
4. При необходимости добавьте новый домен в настройки счетчика

---

## 🛠 Возможные проблемы и решения

### Проблема 1: Форма не отправляется
**Причина:** Неправильный путь к `send.php`

**Решение:**
- Убедитесь, что в JS-файле указан правильный путь: `/send.php`
- Проверьте, что `send.php` загружен в корень сайта

### Проблема 2: Письма не приходят
**Причина:** Настройки mail() на хостинге

**Решение:**
1. В панели Beget перейдите в **"Почта"**
2. Создайте почтовый ящик `noreply@ваш-домен.ru`
3. В `send.php` измените строку:
   ```php
   'From: noreply@ваш-домен.ru',
   ```

### Проблема 3: Ошибка CORS
**Причина:** Браузер блокирует запросы

**Решение:**
Уже добавлено в код `send.php`:
```php
header('Access-Control-Allow-Origin: *');
```

### Проблема 4: Сайт выглядит "сломанным"
**Причина:** Неправильные пути к файлам

**Решение:**
1. Убедитесь, что структура папок сохранена:
   ```
   public_html/
   ├── index.html
   ├── assets/
   ├── send.php
   └── favicon.svg
   ```
2. Проверьте, что загружены ВСЕ файлы из `dist/`

---

## 📞 Нужна помощь?

Если что-то не работает:
1. Проверьте консоль браузера (F12 → Console)
2. Проверьте файл `mail_log.txt` на сервере
3. Обратитесь в поддержку Beget (они помогут с настройкой PHP mail)

---

## ✨ Готово!

Теперь ваш сайт работает на собственном хостинге Beget:
- ✅ Быстрая загрузка (статические файлы)
- ✅ Полный контроль над кодом
- ✅ Отправка формы через PHP
- ✅ Яндекс.Метрика работает
- ✅ Можно редактировать файлы напрямую

**Следующие шаги:**
- Настройте резервное копирование в панели Beget
- Проверьте скорость сайта через PageSpeed Insights
- Добавьте сайт в Яндекс.Вебмастер и Google Search Console

Удачи! 🚀
